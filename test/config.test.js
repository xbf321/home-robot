import test from 'node:test';
import assert from 'node:assert';
import config from '../src/utils/config.js';

test('ConfigTest', async () => {
  // console.info(config);
  // assert.strictEqual(config.tts_engine, 'Edge');
});