import test from 'node:test';
import assert from 'node:assert';

import ASRFactory from '../src/asr.js';
import { ASRProvider } from '../src/utils/constant.js';

const audioFile = '/example/audio-test.mp3';

test('TencentASR', async () => {
  // const asr = ASRFactory.createInstance(ASRProvider.Tencent);
  // const query = await asr.transcribe(audioFile);
  // console.info('TencentASR query:', query);
});